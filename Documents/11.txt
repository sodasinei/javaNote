首先主方法，然后写一个类，类里要求判断奇偶数：类里写一个方法接收值，然后进行常规判断（可用三元运算），结果要有返回，主方法调用这个方法流程：在主方法里创造这个类的对象，因为是不同类的调用，然后用对象调用这个方法并输入值，这个要有boolearn类型的变量来接收结果，然后为了看到结果，要将这个值输出

主方法，再写一个别的类，类里写一个方法，返回类型，方法名，用数组接收，方法体是：定义一个变量表示数组的行数，定义一个变量表示数组的列数，for循环双重打印，完成方法体。在主方法创造一个别的类的对象，对象.方法调用，传入一个数组，之前创造数组的空间大小new int[][]

直接在另一个类的方法的形参接收行数，列数，char c，然后双重for循环<行数，<列数，图像，主方法创建另一个类的对象，然后调用，传入数据

p是找到一个地址，然后根据地址在堆区找到对象的空间，然后如果是字符串类型，则在堆区存的是地址，然后地址指向方法区的数据，如果是基本类型，则在堆区直接存数据

引用传递本质只是把地址复制给了新代表，并没有其他意思

就是又重新创建了一个新对象，并指向它，但结果输出的是在本栈调用的p

因为要求是复制一个Person类的对象，并返回，所以返回类型是Person，类是自定义的数据类型

p2本身是一个地址，所以返回的也是一个地址

在主方法调用一个类的属性，首先创造这个类的对象，然后给属性赋值，再调用另一个类的方法，也是首先创造这个类的对象（属性与方法同等），格式是类名 对象名 = new 类名（）;然后通过对象名.方法名传入一个对象。
方法的实施流程，先写一个类，然后格式public 返回类型 方法名（参数接收）{
方法体
只有对象.方法

每一个方法结束后都会回到原先调用方法的地方
return就是把结果返回到上一个方法中

斐波那契数列思路分析：
1.当n =1,值为1；
2.当n =2,值为1；
3.当n >=3,值为前面两个数的和；
